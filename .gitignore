# 归档的历史版本（不进 git）
archive/

# 系统/编辑器/同步
.DS_Store
*.swp
*~

# 二进制/大文件
*.docx
*.dmg
*.zip

# 临时/冲突文件
*_Conflict.md
*~Conflict*
.~lock.*

# 源格式（已转换为 md）
*.txt

# Lean audit reports (generated, not source)
formal/*.lean.md

# Lean build cache
formal/.lake/
formal/lake-manifest.json

# Spotlight / 系统索引标记
.metadata_never_index
